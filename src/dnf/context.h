#ifndef DNF_CONTEXT_H
#define DNF_CONTEXT_H

// 预留：DNF上下文结构体

typedef struct dnf_context_s {
    // 预留成员
    int dummy;//占位
} dnf_context_t;

dnf_context_t *dnf_context_self(void);

#endif // DNF_CONTEXT_H 